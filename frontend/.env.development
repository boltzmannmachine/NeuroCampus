# URL base del backend local (FastAPI)
VITE_API_BASE=http://127.0.0.1:8000

# Alias opcional (el cliente también lo soporta)
VITE_API_URL=http://127.0.0.1:8000

# Límite de tamaño en MB que valida el frontend antes de enviar archivos
# (coordínalo con NC_MAX_UPLOAD_MB del backend; p.ej., 10)
VITE_MAX_UPLOAD_MB=10
