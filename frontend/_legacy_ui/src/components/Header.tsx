/**
 * Componente de ejemplo para estructurar layout.
 * Mantener simple en Día 1 (sin estilos complejos).
 */
export default function Header() {
  return (
    <header style={{ padding: 12, borderBottom: "1px solid #eee" }}>
      <strong>NeuroCampus</strong>
    </header>
  );
}